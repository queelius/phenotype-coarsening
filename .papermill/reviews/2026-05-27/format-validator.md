# Format Validator Report (2026-05-27)

## Scope

Re-verify build state, label resolution, page count, em-dash freedom, and venue-formatting items. Check whether the 2026-05-23 deferred items (PDF metadata, hyperref warnings, Vancouver style, linenumbers) are still outstanding.

## Build state

- make paper succeeds; main.pdf is current.
- Page count: 20 (target for AMIA is 10 to 12; target for JAMIA is around 5000 words plus figures; current is over both, see F1 below).
- main.log Warnings: 4 (all hyperref "Token not allowed in PDF string" warnings; cosmetic; carryover from 2026-05-23 m10).
- main.log Errors: 0.
- All citations resolve; no undefined references; no overfull/underfull boxes flagged.
- Em-dash freedom: verified by grep for U+2014 (em-dash). None found in main.tex, sections/*.tex, or refs.bib. Hook compliance is intact.

Status: build is clean. The hyperref Token warnings are unchanged from 2026-05-23 m10 and remain cosmetic.

## PDF metadata

```
Title:          (empty)
Subject:        (empty)
Keywords:       (empty)
Author:         (empty)
Creator:        LaTeX with hyperref
Producer:       pdfTeX-1.40.25
```

Status: F2 carryover from 2026-05-23 m6 (suggestion 1). The PDF metadata is still empty. A hypersetup block in main.tex would fix this:
```
\hypersetup{
  pdftitle={Electronic phenotyping as coarsening at random},
  pdfauthor={Alexander Towell},
  pdfkeywords={electronic phenotyping, identifiability, coarsening at random, ICD codes, chart review}
}
```

This is a one-line fix and should land before any venue submission.

## Page count

Current: 20 pages.

- JAMIA target: research article around 5000 words plus figures/tables. The current paper is over budget on text. Compression toward JAMIA will be needed.
- AMIA Annual Symposium: 10 pages including references. Current is 8 pages over. Substantial compression needed for AMIA.
- CHIL: ~8 pages plus references. Need to compress.
- ML4H workshop: ~4 pages, full paper ~8 pages. Need to compress.

The state file's notes flag this as Tier 3 polish (P4: "12-page conference compression"). This is still outstanding.

### F1 (Major, carryover from state file P4): Page count exceeds all primary-target venues.

The paper is 20 pages; the primary targets all need 10 to 12 or fewer. The natural compressions:
- Background section (background.tex, 84 lines) can be cut to a half-page primer.
- Translation table (tab:translation) can be made small or relegated to an appendix in conference versions.
- Validation section (validation.tex, 268 lines) is the longest section and the natural compression target: each of the four studies can be one paragraph plus its table.
- Two figures (glass_ceiling.pdf and informative_coding.pdf) can be combined into one two-panel figure.

Status: page-count compression is the principal Tier-3 polish before any submission. Outstanding.

## Label resolution

All cross-references (cref/Cref/eqref) resolve. The new label cor:casemix is referenced from identifiability.tex line 88, methodology.tex line 152, validation.tex line 180. All resolve cleanly.

The thm/cor labels follow a consistent naming scheme (thm:glass-ceiling, thm:identifiability-chart, thm:code-total, thm:bias-informative, cor:casemix). Good.

## Venue-formatting items (carryovers)

### F3 (Minor, carryover from 2026-05-23 m8): Bibliography style for JAMIA submission.

Current: \bibliographystyle{plainnat}.

JAMIA requires Vancouver style with numbered citations. The current style is author-year. This needs to be switched at submission time but is not urgent for the conference route.

### F4 (Minor, carryover from 2026-05-23 m9): Line numbers for JAMIA submission.

JAMIA submissions require line numbers for reviewer convenience. Add \usepackage{lineno} \linenumbers in main.tex preamble at submission time.

### F5 (Minor, carryover from state file P3): Enlarge red marker in glass-ceiling figure.

The state file flagged this as a polish item; not addressed in 2026-05-27 fix pass. The figure is figures/glass_ceiling.pdf, generated by scripts/figures.R. The red marker for the true parameter is hard to see in the printed PDF.

Suggestion: in scripts/figures.R, increase the marker size for the true-parameter point (cex from default to around 2.5) and use a distinguishing shape (pch=23 filled diamond instead of pch=21).

## New findings

### F6 (Minor, new): No make sim target.

Suggestion 4 from 2026-05-23 ("Add a make sim Makefile target for reproducibility"). Not addressed. Adding:
```
sim: scripts/run.R
	Rscript scripts/run.R
```
to the Makefile would make the simulation runnable in one command.

### F7 (Minor, new): Figure file format consistency.

Figures are PDFs (good). No SVG sources committed alongside; if the journal asks for editable figures at submission time, the .R script will need to be re-run or SVG sources added. Not urgent.

### F8 (Suggestion, new): subcaption package for subfigure refs.

The current figures use (A) and (B) suffix labels manually within the caption. If the venue requires \subref-style subfigure references, \usepackage{subcaption} will be needed. Not urgent.

## Summary

Build is clean; em-dash freedom intact; all labels resolve. The 2026-05-23 deferred items (PDF metadata, hyperref warnings, Vancouver style, line numbers, figure marker, page-count compression) are all still outstanding but were known-deferred. No new format failures introduced by the 2026-05-27 fix pass.

The principal pre-submission format work is page-count compression for the conference route (8 to 10 pages over budget) and the JAMIA-style switches (Vancouver, line numbers, structured PDF metadata).

Severity: 0 critical, 1 major (carryover, page count), 4 minor (carryovers), 2 minor/suggestion (new).
